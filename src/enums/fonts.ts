export enum FontFamily {
  /** Ornate roman serif — titles, win banners */
  Title   = '"Cinzel Decorative", "Times New Roman", serif',
  /** Elegant roman serif — labels (BALANCE, BET) */
  Label   = '"Cinzel", "Georgia", serif',
  /** Condensed bold sans — numbers, SPIN, values */
  Heading = '"Bebas Neue", "Arial Narrow", sans-serif',
  /** Readable sans — small help text */
  Body    = '"Cinzel", Arial, sans-serif',
}

export enum FontSize {
  Xs    = '11px',
  Sm    = '13px',
  Md    = '18px',
  Lg    = '22px',
  Xl    = '28px',
  Xxl   = '36px',
  Hero  = '52px',
}
