export enum UiBox {
  Width       = 220,
  Height      = 90,
  Radius      = 18,
  FillAlpha   = 0.75,
  BorderWidth = 2,
  BorderAlpha = 0.9,
}

export enum SpinBtn {
  R           = 74,    // circle radius
  HitR        = 76,    // hit circle radius
  RingR       = 82,    // outer glow ring radius
  BorderWidth = 5,
  HighlightA  = 0.22,
  ShadowAlpha = 0.5,
}

export enum ReelFrame {
  GlowPad     = 24,
  GlowRadius  = 32,
  GlowAlpha   = 0.14,
}
